cd ..;
addPaths();
cd testSweet;
files = dir;
tests = [];
for n = 1:length(files)
    if
    if strcmp(files(n).name([(end-1):end]),'.m') && ~strcmp(files(n).name,'testAll.m')
        tests = [tests files(n)];
    end
end